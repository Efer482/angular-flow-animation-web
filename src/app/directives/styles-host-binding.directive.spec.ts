import { CssHostBindingDirective } from './styles-host-binding.directive';

describe('StylesHostBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new CssHostBindingDirective();
    expect(directive).toBeTruthy();
  });
});
